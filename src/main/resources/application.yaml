spring:
  application:
    name: LibraryDAW
  datasource:
    # ELIMINA el prefijo "spring.datasource.url=" de aquí dentro
    url: jdbc:mysql://127.0.0.1:3306/bookdb?allowPublicKeyRetrieval=true&useSSL=false
    username: bookuser
    password: bookpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
  sql:
    init:
      mode: always

server:
  port: 8090